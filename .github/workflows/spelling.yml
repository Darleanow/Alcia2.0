name: Spelling

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  codespell:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
        with:
          token: ${{ secrets.GH_PAT || secrets.GITHUB_TOKEN }}
      - uses: codespell-project/actions-codespell@v2
        with:
          check_filenames: true
          check_hidden: false
          path: src include tests docs
